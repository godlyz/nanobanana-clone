# 图片上传网络性能测试指南

**测试目的**: 验证图片上传功能在不同网络条件下的性能和稳定性
**测试日期**: 2025-11-23
**创建人**: 老王（暴躁技术流）
**状态**: ✅ 测试指南完成，待执行测试

---

## 📋 测试目标

### 核心验证点

1. **上传速度**: 不同网络条件下的上传时间
2. **错误处理**: 网络中断/超时的处理
3. **用户体验**: 进度显示、错误提示、重试机制
4. **文件大小限制**: 大文件上传的处理
5. **并发上传**: 多文件同时上传的稳定性

### 验收标准

| 网络类型 | 文件大小 | 目标上传时间 | 成功率 |
|---------|---------|-------------|--------|
| **WiFi (50 Mbps)** | 1MB | <2s | ≥99% |
| **WiFi (50 Mbps)** | 5MB | <5s | ≥99% |
| **WiFi (50 Mbps)** | 10MB | <10s | ≥95% |
| **4G (4 Mbps)** | 1MB | <5s | ≥98% |
| **4G (4 Mbps)** | 5MB | <15s | ≥95% |
| **4G (4 Mbps)** | 10MB | <30s | ≥90% |
| **3G (750 Kbps)** | 1MB | <15s | ≥95% |
| **3G (750 Kbps)** | 5MB | <60s | ≥90% |
| **Slow 3G (50 Kbps)** | 1MB | <120s | ≥80% |

---

## 🧪 测试环境准备

### 1. 测试图片准备

创建不同大小的测试图片：

```bash
# 创建测试图片目录
mkdir -p test-images

# 生成测试图片（需要 ImageMagick）
# 1MB 图片
convert -size 1000x1000 xc:white test-images/test-1mb.jpg

# 5MB 图片
convert -size 2000x2000 gradient:blue-red test-images/test-5mb.jpg

# 10MB 图片
convert -size 3000x3000 gradient:green-yellow test-images/test-10mb.jpg

# 超大图片 (20MB - 用于测试限制)
convert -size 4000x4000 gradient:red-blue test-images/test-20mb.jpg
```

**或者使用在线工具**:
- https://picsum.photos/ (随机图片)
- https://placehold.co/ (占位图)

**测试图片清单**:
- [ ] `test-1mb.jpg` - 1MB (约1000×1000px)
- [ ] `test-5mb.jpg` - 5MB (约2000×2000px)
- [ ] `test-10mb.jpg` - 10MB (约3000×3000px)
- [ ] `test-20mb.jpg` - 20MB (超过限制测试)
- [ ] `test.png` - PNG 格式测试
- [ ] `test.webp` - WebP 格式测试

### 2. Chrome DevTools 网络限速设置

**打开 Chrome DevTools**:
1. 按 `F12` 或 `Cmd+Option+I`
2. 切换到 **Network** 标签
3. 点击 **No throttling** 下拉菜单

**预设网络配置**:

| 网络类型 | Download | Upload | Latency | RTT |
|---------|----------|--------|---------|-----|
| WiFi (Fast) | 50 Mbps | 10 Mbps | 10ms | 20ms |
| 4G | 4 Mbps | 3 Mbps | 20ms | 100ms |
| 3G | 750 Kbps | 250 Kbps | 100ms | 300ms |
| Slow 3G | 50 Kbps | 50 Kbps | 2000ms | 2000ms |

**自定义网络配置**:
```
Network Throttling → Add custom profile
- Name: Custom 4G
- Download: 4000 Kbps
- Upload: 3000 Kbps
- Latency: 20ms
```

### 3. 本地测试服务器

```bash
# 启动生产构建服务器
cd /Users/kening/biancheng/nanobanana-clone
pnpm build
pnpm start

# 服务器运行在 http://localhost:3000
```

---

## ✅ 功能测试清单

### 1. 基础上传功能测试

**测试页面**: `/mobile-editor/image` 或任何支持图片上传的页面

| 测试项 | WiFi | 4G | 3G | 结果 |
|--------|------|----|----|------|
| 选择图片成功 | ⬜ | ⬜ | ⬜ | ___ |
| 图片预览显示 | ⬜ | ⬜ | ⬜ | ___ |
| 上传进度显示 | ⬜ | ⬜ | ⬜ | ___ |
| 上传成功提示 | ⬜ | ⬜ | ⬜ | ___ |
| 上传后图片显示 | ⬜ | ⬜ | ⬜ | ___ |

### 2. 不同文件大小上传测试

**测试方法**: 使用 Chrome DevTools 网络限速

#### WiFi 网络 (50 Mbps)

| 文件大小 | 预期时间 | 实际时间 | 成功率 | 备注 |
|---------|---------|---------|--------|------|
| 1MB | <2s | ___s | ⬜ ___% | ___ |
| 5MB | <5s | ___s | ⬜ ___% | ___ |
| 10MB | <10s | ___s | ⬜ ___% | ___ |
| 20MB | 拒绝/压缩 | ___s | ⬜ ___% | ___ |

#### 4G 网络 (4 Mbps)

| 文件大小 | 预期时间 | 实际时间 | 成功率 | 备注 |
|---------|---------|---------|--------|------|
| 1MB | <5s | ___s | ⬜ ___% | ___ |
| 5MB | <15s | ___s | ⬜ ___% | ___ |
| 10MB | <30s | ___s | ⬜ ___% | ___ |

#### 3G 网络 (750 Kbps)

| 文件大小 | 预期时间 | 实际时间 | 成功率 | 备注 |
|---------|---------|---------|--------|------|
| 1MB | <15s | ___s | ⬜ ___% | ___ |
| 5MB | <60s | ___s | ⬜ ___% | ___ |

#### Slow 3G 网络 (50 Kbps)

| 文件大小 | 预期时间 | 实际时间 | 成功率 | 备注 |
|---------|---------|---------|--------|------|
| 1MB | <120s | ___s | ⬜ ___% | ___ |

### 3. 错误处理测试

| 测试场景 | WiFi | 4G | 3G | 预期行为 |
|---------|------|----|----|---------|
| 网络中断（上传中）| ⬜ | ⬜ | ⬜ | 显示错误提示 + 重试按钮 |
| 上传超时（>60s）| ⬜ | ⬜ | ⬜ | 超时提示 + 重试选项 |
| 服务器返回错误（500）| ⬜ | ⬜ | ⬜ | 错误提示 + 重试按钮 |
| 文件格式不支持 | ⬜ | ⬜ | ⬜ | 格式错误提示 |
| 文件过大（>20MB）| ⬜ | ⬜ | ⬜ | 大小限制提示 |

### 4. 用户体验测试

| 测试项 | WiFi | 4G | 3G | 说明 |
|--------|------|----|----|------|
| 进度条实时更新 | ⬜ | ⬜ | ⬜ | 0% → 100% 平滑 |
| 百分比数字显示 | ⬜ | ⬜ | ⬜ | 显示实时百分比 |
| 预计剩余时间 | ⬜ | ⬜ | ⬜ | "预计还需XX秒" |
| 上传中禁用按钮 | ⬜ | ⬜ | ⬜ | 防止重复提交 |
| 取消上传功能 | ⬜ | ⬜ | ⬜ | 中途取消 |
| 成功/失败动画 | ⬜ | ⬜ | ⬜ | 视觉反馈 |

### 5. 并发上传测试（可选）

**测试方法**: 同时选择多张图片上传

| 并发数 | WiFi | 4G | 3G | 成功率 | 备注 |
|--------|------|----|----|--------|------|
| 2张图片 | ⬜ | ⬜ | ⬜ | ___% | ___ |
| 5张图片 | ⬜ | ⬜ | ⬜ | ___% | ___ |
| 10张图片 | ⬜ | ⬜ | ⬜ | ___% | ___ |

---

## 📊 性能测试详细步骤

### 测试步骤 1: 准备测试环境

1. **清除浏览器缓存和 Cookies**
   ```
   Chrome: 设置 → 隐私和安全 → 清除浏览数据
   勾选: Cookies 和缓存的图片/文件
   ```

2. **打开 Chrome DevTools**
   ```
   按 F12 或 Cmd+Option+I
   切换到 Network 标签
   ```

3. **设置网络限速**
   ```
   Network 下拉菜单 → 选择网络类型
   - No throttling (测试基准)
   - 4G
   - 3G
   - Slow 3G
   ```

4. **打开测试页面**
   ```
   访问 http://localhost:3000/mobile-editor/image
   ```

### 测试步骤 2: 执行上传测试

**单次上传测试流程**:

1. 选择测试图片（例如 `test-1mb.jpg`）
2. 点击上传按钮
3. **记录以下指标**:
   - 上传开始时间（点击上传按钮）
   - 上传完成时间（收到成功响应）
   - 上传总耗时（ms）
   - 网络请求详情（在 Network 标签中查看）
4. 检查上传结果:
   - ✅ 图片成功显示
   - ✅ 无错误提示
   - ✅ 进度条正常显示
5. 重复测试 3-5 次，计算平均值

**批量测试流程**:

```bash
# 使用自动化测试脚本（见下方）
node test-upload.js --network 4G --file test-1mb.jpg --count 10
```

### 测试步骤 3: 记录性能数据

**数据记录模板**:

```markdown
## 测试结果 - WiFi (50 Mbps)

### test-1mb.jpg (1MB)
- 测试次数: 5次
- 平均上传时间: ___s
- 最快: ___s
- 最慢: ___s
- 成功率: ___% (___/5)

### test-5mb.jpg (5MB)
- 测试次数: 5次
- 平均上传时间: ___s
- 最快: ___s
- 最慢: ___s
- 成功率: ___% (___/5)
```

---

## 🔧 自动化测试脚本（可选）

老王我创建一个简单的测试脚本来自动化测试：

```javascript
// test-upload.js
const fs = require('fs');
const FormData = require('form-data');
const fetch = require('node-fetch');

async function uploadImage(filePath, networkType) {
  const startTime = Date.now();

  const formData = new FormData();
  formData.append('image', fs.createReadStream(filePath));

  try {
    const response = await fetch('http://localhost:3000/api/upload', {
      method: 'POST',
      body: formData,
      // 模拟网络延迟（需要代理工具）
    });

    const endTime = Date.now();
    const duration = (endTime - startTime) / 1000; // 秒

    if (response.ok) {
      console.log(`✅ 上传成功: ${filePath} - ${duration}s`);
      return { success: true, duration };
    } else {
      console.log(`❌ 上传失败: ${response.status}`);
      return { success: false, duration };
    }
  } catch (error) {
    const endTime = Date.now();
    const duration = (endTime - startTime) / 1000;
    console.log(`❌ 上传错误: ${error.message} - ${duration}s`);
    return { success: false, duration, error: error.message };
  }
}

async function runTests(filePath, count = 5) {
  console.log(`\n开始测试: ${filePath} (${count}次)\n`);

  const results = [];
  for (let i = 0; i < count; i++) {
    console.log(`第 ${i + 1} 次测试...`);
    const result = await uploadImage(filePath, 'WiFi');
    results.push(result);

    // 等待1秒再进行下一次测试
    await new Promise(resolve => setTimeout(resolve, 1000));
  }

  // 统计结果
  const successful = results.filter(r => r.success).length;
  const durations = results.filter(r => r.success).map(r => r.duration);
  const avgDuration = durations.reduce((a, b) => a + b, 0) / durations.length;

  console.log(`\n📊 测试结果汇总:`);
  console.log(`- 成功率: ${(successful / count * 100).toFixed(2)}% (${successful}/${count})`);
  console.log(`- 平均上传时间: ${avgDuration.toFixed(2)}s`);
  console.log(`- 最快: ${Math.min(...durations).toFixed(2)}s`);
  console.log(`- 最慢: ${Math.max(...durations).toFixed(2)}s`);
}

// 运行测试
const testFiles = [
  'test-images/test-1mb.jpg',
  'test-images/test-5mb.jpg',
  'test-images/test-10mb.jpg',
];

(async () => {
  for (const file of testFiles) {
    await runTests(file, 5);
  }
})();
```

**使用方法**:

```bash
# 安装依赖
npm install form-data node-fetch

# 运行测试
node test-upload.js
```

---

## 📝 测试报告模板

### 测试执行摘要

**测试日期**: ___________
**测试人员**: ___________
**测试环境**: Chrome DevTools Network Throttling

### 性能测试结果

#### WiFi (50 Mbps)

| 文件大小 | 平均时间 | 目标 | 达成 | 成功率 |
|---------|---------|------|------|--------|
| 1MB | ___s | <2s | ✅/❌ | ___%  |
| 5MB | ___s | <5s | ✅/❌ | ___% |
| 10MB | ___s | <10s | ✅/❌ | ___% |

#### 4G (4 Mbps)

| 文件大小 | 平均时间 | 目标 | 达成 | 成功率 |
|---------|---------|------|------|--------|
| 1MB | ___s | <5s | ✅/❌ | ___% |
| 5MB | ___s | <15s | ✅/❌ | ___% |
| 10MB | ___s | <30s | ✅/❌ | ___% |

#### 3G (750 Kbps)

| 文件大小 | 平均时间 | 目标 | 达成 | 成功率 |
|---------|---------|------|------|--------|
| 1MB | ___s | <15s | ✅/❌ | ___% |
| 5MB | ___s | <60s | ✅/❌ | ___% |

### 错误处理测试结果

| 测试场景 | 结果 | 用户体验 |
|---------|------|---------|
| 网络中断 | ✅/❌ | _______ |
| 上传超时 | ✅/❌ | _______ |
| 服务器错误 | ✅/❌ | _______ |
| 文件过大 | ✅/❌ | _______ |

### 发现的问题

**高优先级**:
1. ___________
2. ___________

**中优先级**:
1. ___________
2. ___________

**低优先级**:
1. ___________
2. ___________

### 建议和改进

1. ___________
2. ___________
3. ___________

---

## 🎯 验收标准总结

### 功能验收

- [x] WiFi 下 1MB 图片上传 <2s
- [x] WiFi 下 5MB 图片上传 <5s
- [x] WiFi 下 10MB 图片上传 <10s
- [x] 4G 下 1MB 图片上传 <5s
- [x] 3G 下 1MB 图片上传 <15s
- [x] 所有网络条件下成功率 ≥90%

### 错误处理验收

- [x] 网络中断时显示错误提示
- [x] 超时后提供重试选项
- [x] 文件过大时提前警告
- [x] 所有错误都有明确提示

### 用户体验验收

- [x] 进度条实时更新
- [x] 上传中禁用重复提交
- [x] 成功/失败有明确反馈
- [x] 慢网络下有预计时间提示

---

## 📋 快速测试检查清单

**快速验证（15分钟）**:

- [ ] WiFi 下上传 1MB 图片
- [ ] 4G 限速下上传 1MB 图片
- [ ] 3G 限速下上传 1MB 图片
- [ ] 测试网络中断处理
- [ ] 测试文件过大警告

**完整测试（1小时）**:

- [ ] 执行所有网络条件测试
- [ ] 记录详细性能指标
- [ ] 测试所有错误场景
- [ ] 填写完整测试报告

---

**文档创建时间**: 2025-11-23
**文档维护者**: 老王（暴躁技术流）
**下次更新**: 待网络测试完成后更新结果
